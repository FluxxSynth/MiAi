# =============================================================================
# MiAi — Python Dependencies (pinned)
# =============================================================================
# Install all:  pip install -r requirements.txt
# =============================================================================

# ── Core ─────────────────────────────────────────────────────────────────────
torch>=2.2.0,<3.0.0
transformers>=4.40.0,<5.0.0
datasets>=2.19.0,<3.0.0
accelerate>=0.30.0,<2.0.0
tokenizers>=0.19.0,<1.0.0

# ── PEFT / LoRA ───────────────────────────────────────────────────────────────
peft>=0.10.0,<1.0.0

# ── Web UI ────────────────────────────────────────────────────────────────────
flask>=3.0.0,<4.0.0

# ── Terminal TUI ──────────────────────────────────────────────────────────────
pytermgui>=7.0.0,<8.0.0

# ── Training extras ───────────────────────────────────────────────────────────
rich>=13.7.0,<14.0.0          # Dashboard display
bitsandbytes>=0.43.0,<1.0.0   # QLoRA 4-bit quantization (CUDA only)
trl>=0.8.0,<1.0.0             # DPO alignment training
rouge-score>=0.1.2,<1.0.0     # ROUGE evaluation

# ── RAG (embedding-based retrieval) ───────────────────────────────────────────
# Optional but strongly recommended — upgrades KnowledgeBase from keyword
# matching to semantic cosine similarity (~80 MB model download on first use).
sentence-transformers>=2.7.0,<4.0.0

# ── Performance extras ────────────────────────────────────────────────────────
# flash-attn>=2.5.0   # Flash Attention 2 — uncomment if on CUDA; requires
#                     # a separate build step: pip install flash-attn --no-build-isolation
einops>=0.7.0,<1.0.0  # Required by some model configs
